package com.treemeasurer.measurer.tasks;

import com.treemeasurer.measurer.BaseView;

public interface TasksView extends BaseView {
    // 显示测量胸径的任务
    void showMeasureDbhTask();
    // 显示测量距离的任务
    void showMeasureDisTask();
}
